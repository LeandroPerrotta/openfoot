export enum ServerMessageType {
    GameTimer,

    First = GameTimer,
    Last = GameTimer
}

export enum ClientMessageTypes {
    GameStart,
    GamePause,

    First = GameStart,
    Last = GamePause
}